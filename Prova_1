# Indicare la cartella da cui estrarre i dati
setwd("C:/telerilevamento/") # Windows

# Installazione pacchetto
install.packages("terra")
#analisi e la gestione di dati geospaziali raster e vettoriali

#Funzione per richiamare il pacchetto, ne attiva le funzioni
library(terra)

install.packages("devtools")
#utilizzai i pacchetti in modo più semplice
library(devtools)


install_github("GianlucaNanni/imageRy")
#appartiene al pacchetto "devtools", scaricare e installare un pacchetto direttamente da GitHub

#La stringa "GianlucaNanni/imageRy" indica: 
#GianlucaNanni → il nome dell’utente o dell’organizzazione su GitHub
#imageRy → il nome del pacchetto contenuto nel repository

library(imageRy)
